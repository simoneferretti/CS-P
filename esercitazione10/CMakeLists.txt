cmake_minimum_required(VERSION 3.20)
project(esercitazione10) 

set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wall -Wshadow -Werror=vla -Wno-sign-compare")
set(CMAKE_BUILD_TYPE Release CACHE STRING "Choose the type of build, options are: None Debug Release RelWithDebInfo MinSizeRel" FORCE)

find_package(Eigen3 CONFIG REQUIRED)
list(APPEND libraries_list PUBLIC Eigen3::Eigen)

# Target per test_grad.cpp
add_executable(test_grad test_grad.cpp)
target_link_libraries(test_grad ${libraries_list})

add_test(NAME test_grad COMMAND test_grad)
enable_testing()
